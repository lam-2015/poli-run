<%= form_for(@post) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="row">
      <div class="field col-md-9">
        <%= f.text_field :title, placeholder: 'Summary' %>
        <%= f.text_area :content, placeholder: 'What are you doing?' %>
        <%= f.submit 'Post', class: 'btn btn-primary btn-home' %>
      </div>
    </div>
<% end %>